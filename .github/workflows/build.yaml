name: Automated GitHub Releases

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: omergoktas/qt6bionic:qtbase

    steps:
    - uses: actions/checkout@v3

    - name: Configure the build machine
      shell: bash
      run: |
        apt-get update -y
        apt-get install -y fuse desktop-file-utils patchelf zsync squashfs-tools

    - name: Configure CMake
      shell: bash
      run: cmake -S . -B ../build

    - name: Build
      shell: bash
      run: cmake --build ../build --parallel

    - uses: "marvinpinto/action-automatic-releases@latest"
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "continuous"
        title: "latest"
        prerelease: false
        files: linuxdeployqt-continuous-x86_64.AppImage
